# MIPLET 002: Nomenclature Standardization (indexes)
* **Target Version:** MIP-Core-v1.0.5
* **Target Section:** `## 2. Directory Structure & File Routing` -> `Schema Definitions & Architecture (schemas/)`

### Changes:
1. Rename schema path from `schemas/indices/` to `schemas/indexes/` to enforce uniform nomenclature matching `data/indexes/`.
2. Update all references to index schemas accordingly.
