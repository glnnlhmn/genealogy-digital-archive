{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://genealogy.archive/schemas/entities/location.schema.json",
  "title": "Location",
  "version": "1.0.0",
  "description": "Represents an individual physical facility, landmark, cemetery, or civil jurisdiction entity, utilizing centralized shared definitions.",
  "type": "object",
  "required": [
    "location_id",
    "location_type",
    "location"
  ],
  "properties": {
    "location_id": {
      "$ref": "../defs/_shared_definitions.schema.json#/$defs/location_id"
    },
    "location_type": {
      "type": "string",
      "description": "Classification distinguishing civil jurisdictions from specific physical venues, landmarks, and cemeteries.",
      "enum": [
        "JURISDICTION",
        "CEMETERY",
        "CHURCH",
        "HOSPITAL",
        "FACILITY",
        "RESIDENCE",
        "LANDMARK",
        "OTHER"
      ]
    },
    "location": {
      "$ref": "../defs/_shared_definitions.schema.json#/$defs/location",
      "description": "Standardized and verbatim location container utilizing shared definitions."
    },
    "notes": {
      "type": [
        "array",
        "null"
      ],
      "description": "Structured annotations and historical boundary notes for the location.",
      "items": {
        "$ref": "../defs/_shared_definitions.schema.json#/$defs/note_entry"
      }
    },
    "last_updated": {
      "type": "string",
      "description": "ISO-8601 timestamp representing the last modification to this entity."
    }
  },
  "additionalProperties": false
}